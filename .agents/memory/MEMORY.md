- [LitigaForge country localization](litigaforge-country-localization.md) — per-country pages key off URL path prefix; live UI is built dist served by Node api-server (rebuild+restart to see changes); i18n covers nav/landing only by design.
- [LitigaForge blog + apex /blog](litigaforge-blog-deploy.md) — static Astro→CF Worker; in-process VM timer triggers every 2h; api-server reverse-proxies /blog + dynamic sitemap; FAIL-CLOSED gate (repo var GENERATION_ENABLED) + daily quota (0-publish runs are normal).
- [LitigaForge per-route bot SEO + AEO](litigaforge-seo-per-route.md) — api-server rewrites template per route for bots (title/h1/JSON-LD); _ROUTE_JSONLD injects structured data before </head>; Mediapartners-Google + AdsBot-Google now in _botPattern.
- [LitigaForge judgment slugs & redirects](litigaforge-judgment-redirects.md) — existing slugs NEVER renamed; stale links 301 to canonical (unique-match only, loop-safe); curated cases always-upsert on startup — per-row catch UniqueViolation (source_url partial index) or startup aborts.
- [Stripe + stripe-replit-sync on Replit](stripe-replit-integration.md) — esbuild must externalize the lib (migrations silently skip), connector secret field is `secret` not `secret_key`, seed via code_execution.
- [LitigaForge auth init + PWA gotchas](litigaforge-auth-and-pwa.md) — cookie-only auth; /auth/me refresh-retries on 401; stale PWA SW masks preview; admin = is_superuser via env-var bootstrap on deploy (incl. break-glass password reset).
- [Replit sandbox secrets](replit-sandbox-secrets.md) — code_execution sandbox has NO Replit secrets in process.env; the bash tool env DOES — run scripts that USE a secret value via bash, not code_execution.
- [slowapi + FastAPI body binding](slowapi-fastapi-body-binding.md) — never combine `from __future__ import annotations` with slowapi @limiter.limit on Pydantic-body endpoints; deferred string annotations resolve in slowapi's module → valid JSON bodies 422.
- [GitHub push blocked for main agent](github-push-blocked-main-agent.md) — main agent can't run local git writes (add/commit/push); sync to GitHub origin only via a background Project Task; origin was stale since 2026-05-24 (clean fast-forward behind local).
- [LitigaForge UI gotchas](litigaforge-ui-gotchas.md) — header backdrop-filter traps inline dropdowns (portal menus to body + fixed + rect-anchor, RTL-aware, viewport-clamped); two headers in layout.tsx => duplicate testids.
- [Screenshot litigaforge-ui dev routes](litigaforge-proxy-screenshot.md) — shared proxy routes `/` to api-server's built dist (not Vite), so screenshot tool needs a fresh build; DEV-only routes must be temporarily un-gated, built, shot, then reverted+rebuilt.
- [mockup-sandbox canvas previews](mockup-sandbox-canvas.md) — app_preview screenshots capture ~0.6s after mount (replays intro from t=0), so keep mockup intro animations settling by ~0.6s; reusable SVG gradient/filter ids need useId().
- [LitigaForge IndianKanoon ingestion](litigaforge-judgment-ingest.md) — official paid API only (token-gated, fail-closed); daily 06:00 IST scheduler is PROD-ONLY; manual run_ingestion works in dev (dev DB only — prod has a separate DB → republish/admin-trigger).
- [LitigaForge daily judgment digest](litigaforge-digest.md) — complete subsystem, DON'T rebuild; SMTP now LIVE (Gmail app-password) → double opt-in is the active path; country column (validated, defaults 'in'); prod-only 07:00 IST scheduler; SMTP env read at import → restart on change; prod needs republish.
- [LitigaForge portable LiteLLM layer](litigaforge-litellm-layer.md) — env-switchable (LLM_MODEL); litellm hits the FREE Replit Claude proxy directly (no /v1); LLM_TIMEOUT≥60s; lazy import; live-LLM health probe must be gated; uv packager broken → pip+pin.
- [Iframe aspect-ratio in Vite+Tailwind](iframe-aspect-ratio.md) — inline style aspectRatio causes zero-height wrapper; use Tailwind aspect-video; loading="lazy" on iframes prevents initial render in screenshots.
- [Forge Canvas Phase 2 — dagre + cluster + hints](forge-canvas-phase2.md) — @dagrejs/dagre uses named exports {graphlib, layout}; impact flash via CSS animation injected in injectCSS(); cluster node hides members + reroutes edges; propagateImpact fires only on onConnectTyped (not score slider).
- [LitigaForge presence WebSocket](litigaforge-presence-ws.md) — WS at /litigaforge/ws/cases/{id}; track-view POST fires presence; client-side 15 s expiry + 30 s ping; WS URL from window.location (no env var needed); hooks must live AFTER useQuery in component.
- [LitigaForge lawyer matches shape](litigaforge-lawyer-matches.md) — /matches/lawyer returns {total, matches:[]}; now includes case_requirement_id; matchData?.matches (not Array.isArray) is the right accessor; pendingLeads array was always empty before this fix.
- [LitigaForge NIM embeddings + vision](litigaforge-nim-embeddings.md) — pgvector ivfflat on Replit PG; asyncpg $1::vector string cast; asymmetric e5 (query/passage); judgment search 2-tier cascade (NIM→keyword, no AI fallback); phi-3-vision for image doc analysis (one-shot extract+analyze).

